For some reasons, one may want to ignore pack lines when invoicing.
e.g.: the pack_component_price to 'ignored', so the pack lines prices are equal
to 0.
This is a base module and should be installed in conjuction with corresponding
module from where the invoice is generated.
